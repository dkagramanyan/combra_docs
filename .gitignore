# `sphinx -b doctest docs _doctest` (the CI doctest step) writes here.
_doctest/
__pycache__/

# Per-object API pages written by sphinx autosummary at build time.
docs/api/generated/

# Build artifacts: the rendered site, and the screenshots the run skill takes.
public/
_shots/

# sphinx-gallery output: the example pages, their figures and downloads are
# generated from docs/examples_src/ by the html build, as is the timing page
# and the per-function example lists.
docs/examples/
docs/sg_execution_times.rst
