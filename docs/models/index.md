# Generative models

The four generative model repositories that produce the microstructures combra
scores. They are separate forks that follow one tooling convention, the
{doc}`v2 API convention <spec>`; each has its own page for what is specific to
it.

```{toctree}
:maxdepth: 1

spec
san_v2
styleswin
diffit
edm2
```
